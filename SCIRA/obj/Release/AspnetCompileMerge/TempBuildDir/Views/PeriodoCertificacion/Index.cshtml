@model IEnumerable<SCIRA.Models.c_periodo_certificacion>
@using SCIRA.Utilidades
@{
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
}

@{
    ViewBag.Title = "Periodos de Certificación";
}

<h2>@Strings.getMSG("PeriodoCertificacionIndex001")</h2>

<p>
    @Html.ActionLink(Strings.getMSG("Agregar"), "Create", "PeriodoCertificacion", new { @class = "btn btn-default btn-sm" })
</p>
<table id="results" class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th class="search">@Strings.getMSG("Clave")</th>
            <th class="search">@Strings.getMSG("PeriodoCertificacionIndex002")</th>
            <th class="search">@Strings.getMSG("ProcesosAuditoriaIndexInformes002")</th>
            <th>@Strings.getMSG("IndicadorIndex001")</th>
            <th width="40"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.cl_periodo_certificacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nb_periodo_certificacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.anio)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.esta_activo)
                </td>
                <td>
                    <center>
                        <a href="@Url.Action("Edit", new { id = item.id_periodo_certificacion })">
                            <i class="glyphicon glyphicon-pencil" title="@Strings.getMSG("Editar")"></i>
                        </a>
                        |
                        <a href="@Url.Action("Delete", new { id = item.id_periodo_certificacion })">
                            <i class="glyphicon glyphicon-trash" title="@Strings.getMSG("Borrar")"></i>
                        </a>
                    </center>
                </td>
            </tr>
        }
    </tbody>
</table>
