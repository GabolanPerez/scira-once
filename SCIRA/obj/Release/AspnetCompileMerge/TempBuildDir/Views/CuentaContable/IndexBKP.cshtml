@model IEnumerable<SCIRA.Models.c_cuenta_contable>
@using SCIRA.Utilidades
@{
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
}

@{
    ViewBag.Title = "Cuentas Contables";
}

<h2>@Strings.getMSG("Menu044")</h2>

<p>
    @Html.ActionLink(Strings.getMSG("Agregar"), "Create", "CuentaContable", new { @class = "btn btn-default" })
</p>
<table id="results" class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th class="search">@Strings.getMSG("Clave")</th>
            <th class="search">@Strings.getMSG("BDEICreate052")</th>
            <th class="search">@Strings.getMSG("Entidad")</th>
            <th width="40"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.cl_cuenta_contable)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nb_cuenta_contable)
                </td>
                <td>
                    @( item.c_entidad.cl_entidad + " - " + item.c_entidad.nb_entidad)
                </td>
                <td>
                    <a href="@Url.Action("Edit", new { id = item.id_cuenta_contable })">
                        <i class="glyphicon glyphicon-pencil" title="@Strings.getMSG("Editar")"></i>
                    </a> |
                    <a href="@Url.Action("Delete", new { id = item.id_cuenta_contable })">
                        <i class="glyphicon glyphicon-trash" title="@Strings.getMSG("Borrar")"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
