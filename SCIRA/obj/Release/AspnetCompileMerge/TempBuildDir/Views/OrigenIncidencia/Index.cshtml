@model IEnumerable<SCIRA.Models.c_origen_incidencia>

@{
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
}

@{
    ViewBag.Title = "Origenes de Incidencia";
}

<h2>Orígenes de Incidencia</h2>

<p>
    @Html.ActionLink("Agregar", "Create", "OrigenIncidencia", new { @class = "btn btn-default btn-sm" })
</p>
<table id="results" class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th class="search">Clave</th>
            <th class="search">Origen de Incidencia</th>
            <th width="40"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.cl_origen_incidencia)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nb_origen_incidencia)
                </td>
                <td>
                    <a href="@Url.Action("Edit", new { id = item.id_origen_incidencia })">
                        <i class="glyphicon glyphicon-pencil" title="Editar"></i>
                    </a> |
                    <a href="@Url.Action("Delete", new { id = item.id_origen_incidencia })">
                        <i class="glyphicon glyphicon-trash" title="Borrar"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
