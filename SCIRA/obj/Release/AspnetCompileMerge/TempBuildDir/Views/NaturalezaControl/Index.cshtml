@model IEnumerable<SCIRA.Models.c_naturaleza_control>
@using SCIRA.Utilidades
@{
    ViewBag.Title = "Naturaleza del Control";
}

@{
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
    ViewBag.Title = "Naturaleza del Control";
}

<h2>@Strings.getMSG("Menu032")</h2>

<p>
    @Html.ActionLink(Strings.getMSG("Agregar"), "Create", null, new { @class = "btn btn-default btn-sm" })
</p>

<table id="results" class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th>
                @Strings.getMSG("Clave")
            </th>
            <th>
                @Strings.getMSG("Nombre")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.cl_naturaleza_control)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nb_naturaleza_control)
                </td>
                <td>
                    <center>
                        <a href="@Url.Action("Edit", new { id = item.id_naturaleza_control })">
                            <i class="glyphicon glyphicon-pencil" title="@Strings.getMSG("Editar")"></i>
                        </a> |
                        <a href="@Url.Action("Delete", new { id = item.id_naturaleza_control })">
                            <i class="glyphicon glyphicon-trash" title="@Strings.getMSG("Borrar")"></i>
                        </a>
                    </center>
                </td>
            </tr>
        }
    </tbody>
</table>
