@model IEnumerable<SCIRA.Models.c_area>

@{
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
    ViewBag.Title = "Auditores";
}

<h2>Áreas</h2>

<p>
    @Html.ActionLink("Agregar", "Create", "UsuarioAuditoria", new { @class = "btn btn-default btn-sm" })
</p>
<table id="results" class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th class="search">Clave</th>
            <th class="search">Usuario</th>
            <th width="40"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.cl_area)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nb_area)
                </td>
                <td>
                    <a href="@Url.Action("Edit", new { id = item.id_area })">
                        <i class="glyphicon glyphicon-pencil" title="Editar"></i>
                    </a> |
                    <a href="@Url.Action("Delete", new { id = item.id_area })">
                        <i class="glyphicon glyphicon-trash" title="Borrar"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
