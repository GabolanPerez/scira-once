@model IEnumerable<SCIRA.Models.h_acceso>
@using SCIRA.Utilidades
@{
    ViewBag.Title = "Reporte Accesos";
    Layout = "~/Views/Shared/_LayoutReporteLazyLoading.cshtml";
}


<h2>@Strings.getMSG("ReporteAccesoIndex001")</h2>

<table class="table table-striped table-bordered table-hover lazyLoadingTable" data-reportName="RAccesos">
    
</table>

<script>

    var RAccesosCols = [
        {
            data: 'c1',
            title: '@Strings.getMSG("lyPSeguimientosIndex002")',
            render: function (data, type, item) {
                return item.c1;
            },
        },
        {
            data: 'c2',
            title: '@Strings.getMSG("Usuario")',
            render: function (data, type, item) {
                return item.c2;
            },
        },
        {
            data: 'c3',
            title: '@Strings.getMSG("Función")',
            render: function (data, type, item) {
                return item.c3;
            },
        }//,
        //{
        //    data: null,
        //    render: function (data, type, item) {
        //        return '<button class="btn btn-link btn-sm text-danger js-delete" data-id="' + item.c1 + '" title="Delete this material"><i class="fa fa-trash-alt"></i></button>';
        //    },
        //    orderable: false,
        //    className: 'py-0',
        //},
    ];

    var RAccesosURL = "@Url.Action("ajaxData", "ReporteAcceso")";

    //Si se desea
    RAccesoslengthMenu = [[10, 25, 50, 100, 1000], [10, 25, 50, 100, 1000]];

</script>