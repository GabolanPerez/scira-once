@model IEnumerable<SCIRA.Models.c_linea_negocio_riesgo_operacional>
@using SCIRA.Utilidades
@{
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
    ViewBag.Title = Strings.getMSG("BDEICreate200");
}


<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink(Strings.getMSG("Agregar"), "Create", null, new { @class = "btn btn-default" })
</p>

<table id="results" class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th>
                @Strings.getMSG("Clave")
            </th>
            <th>
                @Strings.getMSG("Nombre")
            </th>
            <th>
                @Strings.getMSG("CategoriaLineaNegocioRiesgoOperacionalIndex001")
            </th>
            <th></th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.cl_linea_negocio_riesgo_operacional)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nb_linea_negocio_riesgo_operacional)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.c_categoria_linea_negocio_riesgo_operacional.nb_categoria_linea_negocio_riesgo_operacional)
                </td>
                <td>
                    <center>
                        <a href="@Url.Action("Edit", new { id = item.id_linea_negocio_riesgo_operacional })">
                            <i class="glyphicon glyphicon-pencil" title="@Strings.getMSG("Editar")"></i>
                        </a> |
                        <a href="@Url.Action("Delete", new { id = item.id_linea_negocio_riesgo_operacional })">
                            <i class="glyphicon glyphicon-trash" title="@Strings.getMSG("Borrar")"></i>
                        </a>
                    </center>
                </td>
            </tr>
        }
    </tbody>
</table>
