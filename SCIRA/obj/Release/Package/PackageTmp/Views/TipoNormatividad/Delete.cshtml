@model SCIRA.Models.c_tipo_normatividad

@{
    ViewBag.Title = "Borrar";
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
}

<h2>Borrar</h2>

<h3>¿Borrar el registro?</h3>
<div>
    <h4>Tipo de Normatividad</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Clave
        </dt>

        <dd>
            @Html.DisplayFor(model => model.cl_tipo_normatividad)
        </dd>

        <dt>
            Nombre
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nb_tipo_normatividad)
        </dd>

    </dl>

    @if (ViewBag.RR.Count > 0)
    {

        <h4>Para poder eliminar éste registro, es necesario eliminar los siguientes elementos:</h4>

        <table id="results" class="table table-striped table-bordered table-hover">
            <thead>
                <tr>
                    <th>Catálogo</th>
                    <th>Clave del Registro</th>
                    <th>Nombre del Registro</th>
                    <th width="40"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in ViewBag.RR)
                {
                    <tr>
                        <td style="white-space: nowrap">
                            @(item.nb_catalogo)
                        </td>
                        <td>
                            @(item.cl_registro)
                        </td>
                        <td>
                            @(item.nb_registro)
                        </td>
                        <td style="white-space: nowrap">
                            <center>
                                <a href="@Url.Action(item.accion,item.controlador, new { id = item.id_registro,redirect = "TipoNormatividad/Delete/" + Model.id_tipo_normatividad})">
                                    <i class="glyphicon glyphicon-trash" title="Borrar"></i>
                                </a>
                            </center>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    @if (ViewBag.RR.Count == 0)
    {
        using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Borrar" class="btn btn-primary" />
            |
            @Html.ActionLink("Regresar", "Return", "Utilidades", new { catalogo = 18 }, new { @class = "btn btn-default btn-sm" })
        </div>
        }
    }
    else
    {
        <div>
            @Html.ActionLink("Regresar", "Return", "Utilidades", new { catalogo = 18 }, new { @class = "btn btn-default btn-sm" })
        </div>
    }
</div>
