@model SCIRA.Models.c_estructura_manual
@using SCIRA.Utilidades
@{
    ViewBag.Title = "Borrar";
}

<h2>@Strings.getMSG("Borrar")</h2>

<h3>@Strings.getMSG("EntidadDelete001")</h3>
<div>
    <h4>@Strings.getMSG("Menu093")</h4>
    <hr />
    
    <div class="row">
        <div class="col-xs-offset-2 col-xs-6 col-md-5">
            <p class="info">
                @Strings.getMSG("EstructuraManualDeleteLvl003")
            </p>
        </div>
    </div>

    <hr />
    
    <dl class="dl-horizontal">
        <dt>
            @Strings.getMSG("Clave")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.cl_estructura_manual)
        </dd>

        <dt>
            @Strings.getMSG("SubProcesoCreate018")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ds_estructura_manual)
        </dd>
    </dl>

    @using (Html.BeginForm(null, null,FormMethod.Post,new { id = Model.id_estructura_manual } ))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="@Strings.getMSG("Borrar")" class="btn btn-primary" id="submit" /> |
            @Html.ActionLink(Strings.getMSG("Regresar"), "Index", new { id = Model.id_estructura_manual }, new { @class = "btn btn-default" })
        </div>
    }
</div>
