@model IEnumerable<SCIRA.Models.c_origen_autoridad>
@using SCIRA.Utilidades
@{
    Layout = "~/Views/Shared/_LayoutIndex.cshtml";
}

@{
    ViewBag.Title = "Orígenes de Autoridad";
}

<h2>@Strings.getMSG("GraphicsOficioIndex005")</h2>

<p>
    @Html.ActionLink(Strings.getMSG("Agregar"), "Create", "OrigenAutoridad", new { @class = "btn btn-default btn-sm" })
</p>
<table id="results" class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th class="search">@Strings.getMSG("Clave")</th>
            <th class="search">@Strings.getMSG("Nombre")</th>
            <th width="40"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.cl_origen_autoridad)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nb_origen_autoridad)
                </td>
                <td>
                    <a href="@Url.Action("Edit", new { id = item.id_origen_autoridad })">
                        <i class="glyphicon glyphicon-pencil" title="@Strings.getMSG("Editar")"></i>
                    </a> |
                    <a href="@Url.Action("Delete", new { id = item.id_origen_autoridad })">
                        <i class="glyphicon glyphicon-trash" title="@Strings.getMSG("Borrar")"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
